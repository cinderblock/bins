#!/bin/sh
# Release entrypoint. The host supervisor launches <app-root>/current/run with
# the release dir as cwd; cd here too so startup migrations resolve
# db/migrations relative to this release. Runs under Bun (the app uses
# bun:sqlite and Bun.serve on a unix socket).
cd "$(dirname "$0")" || exit 1
exec bun server.ts
